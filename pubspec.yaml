name: flutter_application_for_web
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  auto_size_text: ^3.0.0
  carousel_slider: ^4.2.1
  flutter:
    sdk: flutter
  google_fonts: 4.0.4
  responsive_builder: ^0.7.0
  webfeed: ^0.7.0

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - lib/assets/
